[package]
license.workspace = true
name = "moli-process-signal"
version = "0.1.0"
edition = "2024"

[target.'cfg(unix)'.dependencies]
libc = "0.2"
tokio = { version = "1.51.0", features = ["macros", "rt", "signal"] }

[lints]
workspace = true
